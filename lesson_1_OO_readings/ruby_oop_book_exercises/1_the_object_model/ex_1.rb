class Cat
end

socks = Cat.new

=begin
We create an object in Ruby by defining a class using the `class` keyword and the reserved word `end` to
finish the definition. We then instantiate the object (named `socks` in this case), in other words create an instance
(a.k.a. an object), from the `Cat` class by calling the `new` class method
=end